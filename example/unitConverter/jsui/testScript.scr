cmd.includeSuper();

pageLoader.loadPageAndWait("UnitConverter", "ucPage");

cmd.include("testUC.scr");

UnitConverter {
   // client's output
   pageLoader.savePage("UnitConverter", 1, output_c().toString());
   pageLoader.savePage("UnitConverter", 2, output().toString());
}
