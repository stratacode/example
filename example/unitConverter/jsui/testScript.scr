import sc.html.TestPageLoader;

TestPageLoader testLoader = new TestPageLoader(cmd);
testLoader.loadPageAndWait("UnitConverter", "ucPage");

cmd.include("testUC.scr");

UnitConverter {
   // client's output
   testLoader.savePage("UnitConverter", 1, output_c().toString());
   testLoader.savePage("UnitConverter", 2, output().toString());
}
